n = 4;
esPrimo = true;
divisor = 2;
while (divisor < n and esPrimo) {
  if (n % divisor == 0) {
    esPrimo = false;
  }
  divisor = divisor + 1;
}

if (esPrimo) {
  print(n + " es un número primo.");
} else {
  print(n + " no es un número primo.");
}
